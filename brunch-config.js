module.exports = {
    files: {
        javascripts: {
            joinTo: 'app.js'
        }
    },
    paths: {
        watched: ['src'] // use watched this, because default watched directory 'app'
    },
}